import React, {
  useEffect,
  useState,
} from 'react';
import styled from 'styled-components';

const MatchCardToTileModal = () => {

  return (
    <Wrapper>
      <div>MatchCardToTileModal</div>
    </Wrapper>
    );
}

export default MatchCardToTileModal;

const Wrapper = styled.div`

`;